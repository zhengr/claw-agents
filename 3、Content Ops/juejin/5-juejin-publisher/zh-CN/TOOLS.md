# TOOLS.md - 本地备注

- 草稿输入路径。发布日志路径（供数据助手使用）。发布前用 **baoyu-compress-image** 压缩图片。掘金发布：平台 API 或浏览器自动化。安装：`npx skills add <owner/repo> --skill <技能名>`（skills.sh）或 `clawhub install <slug>`（ClawHub）。技能列表见本平台 README。勿存凭证。
