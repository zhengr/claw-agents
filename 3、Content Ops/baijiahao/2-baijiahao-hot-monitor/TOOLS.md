# TOOLS.md - Local Notes

- Search source or API for 百家号 / Baidu hotspots (Playwright / Firecrawl or configured sources).
- Output path for daily or on-demand reports (e.g. `reports/` or `hot-daily/`).
- Industry keywords and topic lists; handoff paths for viral-breakdown and data assistant.
- Report format (fields: trending topics, top article IDs/URLs, summary stats).
- **skills.sh**: baoyu-url-to-markdown, baoyu-format-markdown. Install: `npx skills add <owner/repo> --skill <name>` (skills.sh) or `clawhub install <slug>` (ClawHub). Skill list: see this platform README. Never store credentials here.
