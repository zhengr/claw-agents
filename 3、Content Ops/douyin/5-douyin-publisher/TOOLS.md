# TOOLS.md - Local Notes

- Draft input path. Publish log path for data assistant.
- **baoyu-compress-image** for pre-publish image compression. 抖音 posting: open platform API or browser automation. Install: `npx skills add <owner/repo> --skill <name>` (skills.sh) or `clawhub install <slug>` (ClawHub). Skill list: see this platform README. **Do not store credentials here.**
