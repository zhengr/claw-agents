# TOOLS.md - Local Notes

- Publish log path; report output path; feedback path for viral-breakdown.
- **baoyu-format-markdown** for report format. Install: `npx skills add <owner/repo> --skill <name>` (skills.sh) or `clawhub install <slug>` (ClawHub). Skill list: see this platform README. Never store credentials here.
