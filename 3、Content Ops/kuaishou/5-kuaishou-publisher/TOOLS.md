# TOOLS.md - Local Notes

- Draft input path (approved drafts from rewrite/write).
- Publish log path for data assistant.
- **baoyu-compress-image** before publish. Install: `npx skills add <owner/repo> --skill <name>` (skills.sh) or `clawhub install <slug>` (ClawHub). Skill list: see this platform README. Never store credentials here.
